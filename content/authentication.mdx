---
title: Authentication
description: How sign-in, sessions, and route protection work
icon: key
---

Every route on this site — pages, search, the API playground proxy, even
`llms.txt` — sits behind authentication. There is no anonymous mode.

## The flow

Two sign-in methods are supported; both end in the exact same session, so
everything downstream (middleware, roles, search) doesn't care which one a
user came through.

<Tabs>
  <Tab title="Descope SSO">
    With `DESCOPE_PROJECT_ID` set, the login page shows **Continue with
    Descope SSO**. The app speaks standard OIDC with PKCE to Descope:
    authorize redirect → code exchange → ID-token verification against
    Descope's JWKS (issuer + audience checked, state validated, replay
    rejected). Descope roles in the ID token map to docs roles — anything in
    `DESCOPE_ADMIN_ROLES` (default `admin, docs-admin`) becomes a docs admin;
    everyone else is a member.
  </Tab>
  <Tab title="Local accounts">
    Email + password against the directory in `lib/auth.ts`. Passwords are
    verified against scrypt hashes with per-user salts and constant-time
    comparison — plaintext passwords are never stored. Kept as a break-glass
    path and for environments without Descope.
  </Tab>
</Tabs>

<Steps>
  <Step title="Sign in">
    Via Descope SSO or a local account, as above.
  </Step>
  <Step title="Session cookie">
    On success the server issues a JWT (HS256, signed with `SESSION_SECRET`)
    carrying the user's email, name, and role. It's stored as an HTTP-only,
    `SameSite=Lax` cookie with a 7-day expiry.
  </Step>
  <Step title="Middleware guard">
    `middleware.ts` verifies the token on **every request**. Unauthenticated
    page requests redirect to `/login?next=…`; unauthenticated API requests
    get a `401` JSON response.
  </Step>
</Steps>

## What's protected

| Surface | Behavior without a session |
| --- | --- |
| Doc pages | Redirect to `/login` |
| Search API | `401` |
| Feedback API | `401` |
| `llms.txt` | `401` |
| Smilify API (`/api/v1/*`) | Own bearer-key auth, like any product API |

<Info>
  The Smilify API is deliberately outside the docs session wall — it
  authenticates with an `Authorization: Bearer` key, not your docs cookie,
  exactly as clients consume it in the wild.
</Info>

## Roles

Two roles exist today:

- **member** — sees all standard documentation.
- **admin** — additionally sees groups marked `"access": "admin"` in
  `docs.json` (try the **Admin** section in the sidebar while signed in as an
  admin, then sign in with a member account and watch it disappear).

Role checks apply in three places: sidebar rendering, page access (non-listed
roles get a 404, not a 403 — the page's existence isn't leaked), and search
results.

## Configuring Descope

Set the environment variables (see `.env.example`) and register the redirect
URI in Descope:

```bash title=".env"
DESCOPE_PROJECT_ID=P2xxxxxxxxxxxx      # enables the SSO button
APP_URL=https://docs.internal          # redirect URI base
DESCOPE_ADMIN_ROLES=docs-admin         # Descope roles that grant docs admin
```

Redirect URI to allow in Descope: `{APP_URL}/api/auth/descope/callback`.

<Note>
  The integration is plain OIDC — no vendor SDK. If we ever move off
  Descope, any OIDC-compliant IdP works by pointing `DESCOPE_BASE_URL` and
  the client credentials at it.
</Note>
